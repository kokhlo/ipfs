--  CAR test suite specification
pragma SPARK_Mode;

package CAR_Tests is

   --  Run all CAR tests and print results.
   --  Output format: "CAR tests: N run, M passed"
   procedure Run;

end CAR_Tests;
